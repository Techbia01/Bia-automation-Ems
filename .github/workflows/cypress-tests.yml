name: Pruebas E2E con Cypress

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master, develop ]
  schedule:
    # Ejecutar todos los días a las 2:00 AM UTC
    - cron: '0 2 * * *'
  workflow_dispatch: # Permite ejecución manual

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    name: Ejecutar pruebas Cypress
    
    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Instalar dependencias
        run: npm ci

      - name: Asegurar que el directorio de reportes existe
        run: |
          mkdir -p cypress/reports

      - name: Ejecutar pruebas Cypress
        run: npm run test
        continue-on-error: true
        env:
          CYPRESS_BASE_URL: https://web.dev.bia.app

      - name: Generar reportes después de las pruebas
        if: always()
        run: |
          echo "📊 Generando reportes..."
          npm run merge:reports || echo "⚠️ No se pudieron fusionar reportes (puede ser normal si no hay reportes)"
          npm run generate:report || echo "⚠️ No se pudo generar reporte HTML (puede ser normal si no hay reportes)"

      - name: Publicar reporte HTML
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-report
          path: cypress/reports/report.html
          retention-days: 30

      - name: Publicar videos y screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-artifacts
          path: |
            cypress/videos/**/*.mp4
            cypress/screenshots/**/*.png
          retention-days: 7

      - name: Generar y enviar notificación detallada a Slack
        if: always() && secrets.SLACK_WEBHOOK_URL != null
        env:
          GITHUB_REF_NAME: ${{ github.ref_name }}
          GITHUB_ACTOR: ${{ github.actor }}
          GITHUB_RUN_ID: ${{ github.run_id }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_SERVER_URL: ${{ github.server_url }}
          GITHUB_SHA: ${{ github.sha }}
          GITHUB_WORKFLOW: ${{ github.workflow }}
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          echo "🔔 Preparando notificación a Slack..."
          
          # Verificar que el script existe
          if [ ! -f "scripts/generate-slack-message.js" ]; then
            echo "❌ Error: No se encontró el script generate-slack-message.js"
            exit 1
          fi
          
          # Verificar que el webhook URL está configurado
          if [ -z "$SLACK_WEBHOOK_URL" ]; then
            echo "⚠️ SLACK_WEBHOOK_URL no está configurado. Saltando notificación."
            exit 0
          fi
          
          # Generar payload JSON usando el script de Node.js
          echo "📝 Generando mensaje de Slack..."
          PAYLOAD=$(node scripts/generate-slack-message.js)
          
          if [ -z "$PAYLOAD" ]; then
            echo "⚠️ No se pudo generar el payload. Enviando notificación básica..."
            PAYLOAD=$(cat <<EOF
          {
            "text": "🧪 Reporte de Pruebas Cypress - EMS",
            "blocks": [
              {
                "type": "section",
                "text": {
                  "type": "mrkdwn",
                  "text": "*🧪 Reporte de Pruebas Cypress*\n\n*Repositorio:* $GITHUB_REPOSITORY\n*Rama:* $GITHUB_REF_NAME\n*Commit:* ${GITHUB_SHA:0:7}\n*Ejecutado por:* $GITHUB_ACTOR\n\n<${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}|📋 Ver ejecución completa>"
                }
              }
            ]
          }
          EOF
          )
          fi
          
          # Enviar a Slack
          echo "📤 Enviando notificación a Slack..."
          HTTP_CODE=$(curl -s -o /tmp/slack_response.txt -w "%{http_code}" \
            -X POST \
            -H 'Content-type: application/json' \
            --data "$PAYLOAD" \
            "$SLACK_WEBHOOK_URL")
          
          if [ "$HTTP_CODE" = "200" ]; then
            echo "✅ Notificación enviada exitosamente a Slack"
          else
            echo "⚠️ Error al enviar notificación. Código HTTP: $HTTP_CODE"
            cat /tmp/slack_response.txt || true
            # No fallar el workflow si la notificación falla
          fi
