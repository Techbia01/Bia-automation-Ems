name: Pruebas E2E con Cypress

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master, develop ]
  schedule:
    # Ejecutar todos los días a las 2:00 AM UTC
    - cron: '0 2 * * *'
  workflow_dispatch: # Permite ejecución manual

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    name: Ejecutar pruebas Cypress
    
    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Instalar dependencias
        run: npm ci

      - name: Asegurar que el directorio de reportes existe
        run: |
          mkdir -p cypress/reports

      - name: Ejecutar pruebas Cypress
        run: npm run test 2>&1 | tee cypress-test-output.log
        continue-on-error: true
        env:
          CYPRESS_BASE_URL: https://web.dev.bia.app
          NODE_TLS_REJECT_UNAUTHORIZED: 0
          CYPRESS_VERIFY_TIMEOUT: 120000

      - name: Generar reportes después de las pruebas
        if: always()
        run: |
          echo "📊 Generando reportes..."
          echo "📂 Contenido del directorio cypress/reports:"
          ls -la cypress/reports/ || echo "El directorio no existe o está vacío"
          
          # Verificar si hay archivos JSON de reporte
          echo "🔍 Buscando archivos JSON..."
          JSON_FILES=$(find cypress/reports -name "*.json" -type f 2>/dev/null | head -10)
          if [ -n "$JSON_FILES" ]; then
            echo "✅ Se encontraron archivos JSON de reporte:"
            echo "$JSON_FILES"
            echo ""
            echo "🔄 Fusionando reportes..."
            npm run merge:reports || echo "⚠️ No se pudieron fusionar reportes"
            
            # Verificar si se generó el report.json
            if [ -f "cypress/reports/report.json" ]; then
              echo "✅ report.json generado exitosamente"
              echo "📏 Tamaño del archivo: $(du -h cypress/reports/report.json | cut -f1)"
              echo "🔄 Generando reporte HTML..."
              npm run generate:report || echo "⚠️ No se pudo generar reporte HTML"
            else
              echo "⚠️ No se generó report.json después del merge"
              echo "📂 Archivos en cypress/reports después del merge:"
              ls -la cypress/reports/ || true
            fi
          else
            echo "⚠️ No se encontraron archivos JSON de reporte."
            echo "📂 Buscando en otras ubicaciones posibles..."
            find . -name "*.json" -path "*/cypress/*" -type f 2>/dev/null | head -5 || echo "No se encontraron archivos JSON"
          fi

      - name: Publicar reporte HTML
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-report
          path: cypress/reports/report.html
          retention-days: 30
          if-no-files-found: warn

      - name: Publicar videos y screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-artifacts
          path: |
            cypress/videos/**/*.mp4
            cypress/screenshots/**/*.png
          retention-days: 7

      - name: Generar y enviar notificación detallada a Slack
        if: always()
        continue-on-error: true
        env:
          GITHUB_REF_NAME: ${{ github.ref_name }}
          GITHUB_ACTOR: ${{ github.actor }}
          GITHUB_RUN_ID: ${{ github.run_id }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_SERVER_URL: ${{ github.server_url }}
          GITHUB_SHA: ${{ github.sha }}
          GITHUB_WORKFLOW: ${{ github.workflow }}
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          echo "🔔 Preparando notificación a Slack..."
          echo "📋 Variables de entorno:"
          echo "  GITHUB_REPOSITORY: $GITHUB_REPOSITORY"
          echo "  GITHUB_REF_NAME: $GITHUB_REF_NAME"
          echo "  GITHUB_RUN_ID: $GITHUB_RUN_ID"
          echo "  SLACK_WEBHOOK_URL configurado: $([ -z "$SLACK_WEBHOOK_URL" ] && echo 'NO' || echo 'SÍ')"
          
          # Verificar que el webhook URL está configurado
          if [ -z "$SLACK_WEBHOOK_URL" ]; then
            echo "❌ ERROR: SLACK_WEBHOOK_URL no está configurado."
            echo "   Ve a Settings → Secrets → Actions y configura SLACK_WEBHOOK_URL"
            exit 0  # No fallar el workflow, solo saltar
          fi
          
          echo "✅ Webhook URL configurado correctamente"
          
          # Verificar que el script existe
          if [ ! -f "scripts/generate-slack-message.js" ]; then
            echo "⚠️ No se encontró el script, usando notificación básica"
            USE_BASIC=true
          else
            USE_BASIC=false
          fi
          
          # Generar payload JSON usando el script de Node.js
          echo "📝 Generando mensaje de Slack..."
          echo "📂 Verificando si existe report.json..."
          if [ -f "cypress/reports/report.json" ]; then
            echo "✅ Reporte JSON encontrado"
            ls -lh cypress/reports/report.json
          else
            echo "⚠️ Reporte JSON no encontrado, buscando archivos JSON..."
            ls -la cypress/reports/*.json 2>/dev/null || echo "No se encontraron archivos JSON"
          fi
          
          # Ejecutar script y capturar output
          echo "Ejecutando script de generación de mensaje..."
          SCRIPT_OUTPUT=$(node scripts/generate-slack-message.js 2> /tmp/script_errors.txt)
          PAYLOAD_EXIT_CODE=$?
          
          # Mostrar errores si los hay (pero no fallar)
          if [ -s /tmp/script_errors.txt ]; then
            echo "ℹ️ Mensajes del script:"
            cat /tmp/script_errors.txt
          fi
          
          # Intentar usar el output del script si existe y no está vacío
          if [ $PAYLOAD_EXIT_CODE -eq 0 ] && [ -n "$SCRIPT_OUTPUT" ] && echo "$SCRIPT_OUTPUT" | grep -q '{'; then
            PAYLOAD="$SCRIPT_OUTPUT"
            echo "✅ Payload generado exitosamente desde script"
            echo "Tamaño: $(echo "$PAYLOAD" | wc -c) caracteres"
          else
            echo "⚠️ Usando notificación básica (script falló o no generó output válido)"
            PAYLOAD=$(cat <<EOF
{
  "text": "🧪 Reporte de Pruebas Cypress - EMS",
  "blocks": [
    {
      "type": "section",
      "text": {
        "type": "mrkdwn",
        "text": "*🧪 Reporte de Pruebas Cypress*\n\n*Repositorio:* $GITHUB_REPOSITORY\n*Rama:* $GITHUB_REF_NAME\n*Commit:* ${GITHUB_SHA:0:7}\n*Ejecutado por:* $GITHUB_ACTOR\n\n<${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}|📋 Ver ejecución completa>"
      }
    }
  ]
}
EOF
)
          fi
          
          # Enviar a Slack
          echo "📤 Enviando notificación a Slack..."
          echo "🔗 Webhook URL (primeros 50 caracteres): ${SLACK_WEBHOOK_URL:0:50}..."
          echo "📏 Tamaño del payload: $(echo "$PAYLOAD" | wc -c) caracteres"
          
          HTTP_CODE=$(curl -s -o /tmp/slack_response.txt -w "%{http_code}" \
            -X POST \
            -H 'Content-type: application/json' \
            --data "$PAYLOAD" \
            --max-time 10 \
            --fail-with-body \
            "$SLACK_WEBHOOK_URL" 2>&1)
          
          CURL_EXIT=$?
          
          echo "📊 Resultado de curl:"
          echo "  Exit code: $CURL_EXIT"
          echo "  HTTP code: $HTTP_CODE"
          
          if [ $CURL_EXIT -ne 0 ]; then
            echo "❌ Error de conexión al enviar a Slack"
            echo "Error completo:"
            cat /tmp/slack_response.txt || true
          elif [ "$HTTP_CODE" = "200" ]; then
            echo "✅ Notificación enviada exitosamente a Slack (HTTP 200)"
            echo "Respuesta de Slack:"
            cat /tmp/slack_response.txt || echo "(sin respuesta)"
          else
            echo "⚠️ Error al enviar notificación. Código HTTP: $HTTP_CODE"
            echo "Respuesta de Slack:"
            cat /tmp/slack_response.txt || true
            echo ""
            echo "Payload enviado (primeros 200 caracteres):"
            echo "$PAYLOAD" | head -c 200
            echo "..."
          fi
